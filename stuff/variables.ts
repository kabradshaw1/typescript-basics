let color: string[] = ['red', 'green', 'blue']

class Car {

}

let car: Car = new Car();

// object literial
let point: { x: number, y: number} = {
  x: 10,
  y: 20
}

const logNumber: (i:number) => void = (i: number) => {
  console.log(i)
}